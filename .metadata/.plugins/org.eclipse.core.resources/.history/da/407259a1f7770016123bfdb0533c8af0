package adapter;

import java.io.File;
import java.io.FileWriter;
import java.io.IOException;
import java.io.PrintWriter;
import java.text.DecimalFormat;
import java.util.Random;
import java.util.Scanner;

public class InitInput {
	public static final String PATH_OUTPUT_FILE = "D:\\ToiletSeat\\Test";

	public static void main(String[] args) throws IOException {
		Scanner inp = new Scanner(System.in);
		DecimalFormat format = new DecimalFormat("0.00");
		for (int index = 1; index <= 10; index++) {
//			System.out.print("Nhập số phần tử: ");
			int numberOfInteger = 8;
			String path = PATH_OUTPUT_FILE;
			if (index != 10) {
				path += "0" + index;
			} else {
				path += index;
			}
			File file = new File(path);
			file.mkdirs();
			try (PrintWriter writer = new PrintWriter(new FileWriter(file + "\\ToiletSeat.out", false), true)) {
//				writer.println(numberOfInteger);
//				Random random = new Random();
//				for (int i = 0; i < numberOfInteger; i++) {
//					for(int j = 0; j < 8; j++){
//						int x = random.nextInt(2);
//						if (x == 0) {
//							writer.print("*");
//						}else {
//							writer.print(".");
//						}
//					}
//					writer.println();
//				}
				writer.print("invalid");
				System.out.println("Done: " + index);
			} catch (IOException e) {
				System.out.println(e.getMessage());
				System.exit(1);
			}
		}
		inp.close();
	}
}
